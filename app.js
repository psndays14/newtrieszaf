const STORAGE_KEY="zafbat_sprint1_js_v1";
const S={route:{name:"dashboard",projectId:null},projects:[],documents:[],filters:{search:"",status:"all",sort:"updated_desc"},flash:null};
function uid(prefix="id"){return `${prefix}_${Math.random().toString(36).slice(2,10)}_${Date.now().toString(36)}`;}
function nowISO(){return new Date().toISOString();}
function today(){return new Date().toISOString().slice(0,10);}
function esc(v=""){return String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;");}
function safeNum(v,fallback=0){const n=Number(v);return Number.isFinite(n)?n:fallback;}
function formatMoney(v){const n=safeNum(v,0);return `${n.toLocaleString("fr-FR",{minimumFractionDigits:2,maximumFractionDigits:2})} MAD`;}
function statusClass(status=""){const s=status.toLowerCase();if(s.includes("termin")) return "ok";if(s.includes("cours")||s.includes("étude")||s.includes("etude")||s.includes("prospect")) return "warn";if(s.includes("perdu")||s.includes("suspendu")) return "danger";return "neutral";}
function flash(message,kind="ok"){S.flash={message,kind,at:Date.now()};render();}
function relativeDateLabel(iso){if(!iso) return "—";const d=new Date(iso);if(Number.isNaN(d.getTime())) return "—";return d.toLocaleString("fr-FR");}
function baseProject(){return{id:uid("prj"),ref:"",nom:"",client:"",contact:"",telephone:"",email:"",adresse:"",ville:"Casablanca",type:"Villa",surface:"",budget:0,responsable:"",statut:"Prospect",dateDebut:"",dateFinPrevue:"",notes:"",createdAt:nowISO(),updatedAt:nowISO(),schemaVersion:2};}
function validateEmail(v){if(!v) return true;return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);}
function projectById(id){return S.projects.find(p=>p.id===id)||null;}
function docsForProject(projectId){return S.documents.filter(d=>d.projectId===projectId);}
function saveState(){localStorage.setItem(STORAGE_KEY,JSON.stringify({route:S.route,projects:S.projects,documents:S.documents,filters:S.filters,savedAt:nowISO(),version:1}));}
function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);if(!raw){seedDemo();return;}const parsed=JSON.parse(raw);S.route=parsed.route||S.route;S.projects=Array.isArray(parsed.projects)?parsed.projects:[];S.documents=Array.isArray(parsed.documents)?parsed.documents:[];S.filters=parsed.filters||S.filters;}catch(e){console.error(e);seedDemo();}}
function seedDemo(){const p1=baseProject();p1.ref="ZB-2026-001";p1.nom="Villa Bouskoura";p1.client="Famille El Idrissi";p1.contact="M. El Idrissi";p1.telephone="+212 6 00 00 00 00";p1.email="client@example.com";p1.adresse="Bouskoura, Casablanca";p1.surface=540;p1.budget=4800000;p1.responsable="Yassine";p1.statut="En étude";p1.dateDebut=today();p1.updatedAt=nowISO();
const p2=baseProject();p2.ref="ZB-2026-002";p2.nom="Villa Californie";p2.client="Mme A.";p2.responsable="Yassine";p2.statut="Prospect";p2.budget=6200000;p2.updatedAt=nowISO();
S.projects=[p1,p2];
S.documents=[{id:uid("doc"),type:"devis",projectId:p1.id,projectName:p1.nom,documentRef:"DEV-ZB-2026-001-V1",name:"Devis initial",version:1,status:"final",createdAt:nowISO(),updatedAt:nowISO()},{id:uid("doc"),type:"audit",projectId:p1.id,projectName:p1.nom,documentRef:"AUD-ZB-2026-001-V1",name:"Audit préliminaire",version:1,status:"brouillon",createdAt:nowISO(),updatedAt:nowISO()}];
saveState();}
function exportBackup(){saveState();const raw=localStorage.getItem(STORAGE_KEY)||"{}";const blob=new Blob([raw],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`zafbat-sprint1-backup-${today()}.json`;a.click();URL.revokeObjectURL(a.href);flash("Backup JSON exporté.");}
function importBackup(event){const file=event.target.files?.[0];if(!file) return;const reader=new FileReader();reader.onload=()=>{try{const parsed=JSON.parse(String(reader.result||"{}"));localStorage.setItem(STORAGE_KEY,JSON.stringify(parsed));loadState();S.route={name:"dashboard",projectId:null};flash("Backup importé.");}catch(e){flash("Le fichier JSON est invalide.","error");}};reader.readAsText(file);}
function resetProjectForm(){const ids=["f_ref","f_nom","f_client","f_contact","f_telephone","f_email","f_adresse","f_ville","f_type","f_surface","f_budget","f_responsable","f_statut","f_dateDebut","f_dateFinPrevue","f_notes"];ids.forEach(id=>{const el=document.getElementById(id);if(!el) return;if(id==="f_ville") el.value="Casablanca";else if(id==="f_type") el.value="Villa";else if(id==="f_statut") el.value="Prospect";else el.value="";});}
function createProjectFromForm(){const p=baseProject();p.ref=document.getElementById("f_ref").value.trim();p.nom=document.getElementById("f_nom").value.trim();p.client=document.getElementById("f_client").value.trim();p.contact=document.getElementById("f_contact").value.trim();p.telephone=document.getElementById("f_telephone").value.trim().replace(/\s+/g," ");p.email=document.getElementById("f_email").value.trim();p.adresse=document.getElementById("f_adresse").value.trim();p.ville=document.getElementById("f_ville").value.trim()||"Casablanca";p.type=document.getElementById("f_type").value.trim()||"Villa";p.surface=document.getElementById("f_surface").value.trim()===""?"":safeNum(document.getElementById("f_surface").value,0);p.budget=document.getElementById("f_budget").value.trim()===""?0:safeNum(document.getElementById("f_budget").value,0);p.responsable=document.getElementById("f_responsable").value.trim();p.statut=document.getElementById("f_statut").value;p.dateDebut=document.getElementById("f_dateDebut").value;p.dateFinPrevue=document.getElementById("f_dateFinPrevue").value;p.notes=document.getElementById("f_notes").value.trim();
if(!p.ref||!p.nom||!p.client){flash("Référence, nom du projet et client sont obligatoires.","error");return;}
if(S.projects.some(x=>x.ref.toLowerCase()===p.ref.toLowerCase())){flash("La référence projet doit être unique.","error");return;}
if(!validateEmail(p.email)){flash("L’adresse email n’est pas valide.","error");return;}
if(safeNum(p.budget,0)<0||(p.surface!==""&&safeNum(p.surface,0)<0)){flash("Budget et surface doivent être positifs.","error");return;}
S.projects.unshift(p);saveState();resetProjectForm();flash("Projet créé.");}
function setRoute(name,projectId=null){S.route={name,projectId};saveState();render();}
function filteredProjects(){let items=[...S.projects];const q=S.filters.search.trim().toLowerCase();const st=S.filters.status;if(q){items=items.filter(p=>[p.ref,p.nom,p.client].some(v=>String(v||"").toLowerCase().includes(q)));}if(st!=="all"){items=items.filter(p=>p.statut===st);}if(S.filters.sort==="updated_desc"){items.sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));}else if(S.filters.sort==="updated_asc"){items.sort((a,b)=>new Date(a.updatedAt)-new Date(b.updatedAt));}else if(S.filters.sort==="budget_desc"){items.sort((a,b)=>safeNum(b.budget,0)-safeNum(a.budget,0));}else if(S.filters.sort==="budget_asc"){items.sort((a,b)=>safeNum(a.budget,0)-safeNum(b.budget,0));}return items;}
function dashboardStats(){const active=S.projects.filter(p=>!["Terminé","Perdu"].includes(p.statut)).length;const lastSaved=JSON.parse(localStorage.getItem(STORAGE_KEY)||"{}")?.savedAt||null;return{totalProjects:S.projects.length,activeProjects:active,totalDocuments:S.documents.length,lastSaved};}
function projectFormHTML(){return `<div class="grid grid-2"><div class="field"><label>Référence projet *</label><input id="f_ref" placeholder="ZB-2026-003" /></div><div class="field"><label>Nom du projet *</label><input id="f_nom" placeholder="Villa Anfa" /></div><div class="field"><label>Client *</label><input id="f_client" placeholder="Nom du client" /></div><div class="field"><label>Contact</label><input id="f_contact" placeholder="Contact principal" /></div><div class="field"><label>Téléphone</label><input id="f_telephone" placeholder="+212 ..." /></div><div class="field"><label>Email</label><input id="f_email" type="email" placeholder="client@email.com" /></div><div class="field"><label>Adresse</label><input id="f_adresse" placeholder="Adresse chantier" /></div><div class="field"><label>Ville</label><input id="f_ville" value="Casablanca" /></div><div class="field"><label>Type</label><input id="f_type" value="Villa" /></div><div class="field"><label>Surface (m²)</label><input id="f_surface" type="number" min="0" /></div><div class="field"><label>Budget (MAD)</label><input id="f_budget" type="number" min="0" step="0.01" /></div><div class="field"><label>Responsable</label><input id="f_responsable" placeholder="Chef de projet" /></div><div class="field"><label>Statut</label><select id="f_statut"><option>Prospect</option><option>En étude</option><option>En cours</option><option>Terminé</option><option>Perdu</option><option>Suspendu</option></select></div><div class="field"><label>Date début</label><input id="f_dateDebut" type="date" /></div><div class="field"><label>Date fin prévue</label><input id="f_dateFinPrevue" type="date" /></div><div class="field" style="grid-column:1/-1"><label>Notes</label><textarea id="f_notes" placeholder="Contexte, contraintes, remarques"></textarea></div></div><div class="toolbar" style="margin-top:14px"><button class="btn-primary" onclick="createProjectFromForm()">Créer</button><button class="btn-ghost" onclick="resetProjectForm()">Reset</button></div>`;}
function flashHTML(){if(!S.flash) return "";return `<div class="flash ${esc(S.flash.kind||"info")}">${esc(S.flash.message)}</div>`;}
function dashboardHTML(){const stats=dashboardStats();const items=filteredProjects();return `${flashHTML()}<div class="topbar"><div><h2>Tableau de bord</h2><p>Projets et documents — données locales au navigateur, export recommandé.</p></div><div class="top-actions"><button class="btn-secondary" onclick="exportBackup()">Exporter JSON</button><label class="btn-secondary" style="display:inline-flex;align-items:center;gap:8px;cursor:pointer;">Importer JSON<input type="file" accept="application/json" style="display:none" onchange="importBackup(event)" /></label><button class="btn-primary" onclick="focusNewProject()">Nouveau projet</button></div></div><div class="grid grid-4"><div class="kpi"><div class="label">Projets</div><div class="value">${stats.totalProjects}</div></div><div class="kpi"><div class="label">Projets actifs</div><div class="value">${stats.activeProjects}</div></div><div class="kpi"><div class="label">Documents</div><div class="value">${stats.totalDocuments}</div></div><div class="kpi"><div class="label">Dernière sauvegarde</div><div class="value" style="font-size:1rem">${stats.lastSaved?esc(relativeDateLabel(stats.lastSaved)):"—"}</div></div></div><div class="grid grid-2" style="margin-top:18px"><div class="card"><div class="section-head"><h3>Projets</h3><span class="muted small">${items.length} résultat(s)</span></div><div class="row" style="margin-bottom:14px"><div class="field"><label>Recherche</label><input value="${esc(S.filters.search)}" oninput="updateSearch(this.value)" placeholder="Réf, projet, client" /></div><div class="field"><label>Statut</label><select onchange="updateStatusFilter(this.value)">${["all","Prospect","En étude","En cours","Terminé","Perdu","Suspendu"].map(v=>`<option ${S.filters.status===v?"selected":""} value="${esc(v)}">${esc(v==="all"?"Tous":v)}</option>`).join("")}</select></div><div class="field"><label>Tri</label><select onchange="updateSort(this.value)"><option value="updated_desc" ${S.filters.sort==="updated_desc"?"selected":""}>Dernière mise à jour ↓</option><option value="updated_asc" ${S.filters.sort==="updated_asc"?"selected":""}>Dernière mise à jour ↑</option><option value="budget_desc" ${S.filters.sort==="budget_desc"?"selected":""}>Budget ↓</option><option value="budget_asc" ${S.filters.sort==="budget_asc"?"selected":""}>Budget ↑</option></select></div></div>${items.length?`<div class="table-wrap"><table><thead><tr><th>Réf</th><th>Projet</th><th>Client</th><th>Statut</th><th>Responsable</th><th>Budget</th><th>Mise à jour</th><th>Actions</th></tr></thead><tbody>${items.map(p=>`<tr><td>${esc(p.ref)}</td><td>${esc(p.nom)}</td><td>${esc(p.client)}</td><td><span class="badge ${statusClass(p.statut)}">${esc(p.statut)}</span></td><td>${esc(p.responsable||"—")}</td><td>${formatMoney(p.budget||0)}</td><td>${esc(relativeDateLabel(p.updatedAt))}</td><td><div class="actions-inline"><button class="btn-secondary btn-small" onclick="openProject('${p.id}')">Ouvrir</button><button class="btn-ghost btn-small" onclick="openProject('${p.id}'); setTimeout(() => scrollToDocActions(), 30)">Créer document</button></div></td></tr>`).join("")}</tbody></table></div>`:`<div class="empty">Aucun projet trouvé avec ces filtres.</div>`}</div><div class="card" id="new-project-card"><div class="section-head"><h3>Nouveau projet</h3><span class="muted small">Schéma Project v2</span></div>${projectFormHTML()}</div></div>`;}
function openProject(projectId){setRoute("project",projectId);}
function updateSearch(v){S.filters.search=v;saveState();render();}
function updateStatusFilter(v){S.filters.status=v;saveState();render();}
function updateSort(v){S.filters.sort=v;saveState();render();}
function focusNewProject(){const el=document.getElementById("new-project-card");if(el) el.scrollIntoView({behavior:"smooth",block:"start"});setTimeout(()=>document.getElementById("f_ref")?.focus(),180);}
function projectEditFormHTML(p){return `<div class="grid grid-2"><div class="field"><label>Référence projet *</label><input id="e_ref" value="${esc(p.ref)}" /></div><div class="field"><label>Nom du projet *</label><input id="e_nom" value="${esc(p.nom)}" /></div><div class="field"><label>Client *</label><input id="e_client" value="${esc(p.client)}" /></div><div class="field"><label>Contact</label><input id="e_contact" value="${esc(p.contact||"")}" /></div><div class="field"><label>Téléphone</label><input id="e_telephone" value="${esc(p.telephone||"")}" /></div><div class="field"><label>Email</label><input id="e_email" type="email" value="${esc(p.email||"")}" /></div><div class="field"><label>Adresse</label><input id="e_adresse" value="${esc(p.adresse||"")}" /></div><div class="field"><label>Ville</label><input id="e_ville" value="${esc(p.ville||"")}" /></div><div class="field"><label>Type</label><input id="e_type" value="${esc(p.type||"")}" /></div><div class="field"><label>Surface (m²)</label><input id="e_surface" type="number" min="0" value="${esc(p.surface??"")}" /></div><div class="field"><label>Budget (MAD)</label><input id="e_budget" type="number" min="0" step="0.01" value="${esc(p.budget??0)}" /></div><div class="field"><label>Responsable</label><input id="e_responsable" value="${esc(p.responsable||"")}" /></div><div class="field"><label>Statut</label><select id="e_statut">${["Prospect","En étude","En cours","Terminé","Perdu","Suspendu"].map(v=>`<option ${p.statut===v?"selected":""}>${esc(v)}</option>`).join("")}</select></div><div class="field"><label>Date début</label><input id="e_dateDebut" type="date" value="${esc(p.dateDebut||"")}" /></div><div class="field"><label>Date fin prévue</label><input id="e_dateFinPrevue" type="date" value="${esc(p.dateFinPrevue||"")}" /></div><div class="field" style="grid-column:1/-1"><label>Notes</label><textarea id="e_notes">${esc(p.notes||"")}</textarea></div></div><div class="toolbar" style="margin-top:14px"><button class="btn-primary" onclick="saveProjectEdits('${p.id}')">Enregistrer</button><button class="btn-ghost" onclick="render()">Annuler modifications</button></div>`;}
function saveProjectEdits(projectId){const project=projectById(projectId);if(!project){flash("Projet introuvable.","error");return;}const candidate=structuredClone(project);candidate.ref=document.getElementById("e_ref").value.trim();candidate.nom=document.getElementById("e_nom").value.trim();candidate.client=document.getElementById("e_client").value.trim();candidate.contact=document.getElementById("e_contact").value.trim();candidate.telephone=document.getElementById("e_telephone").value.trim().replace(/\s+/g," ");candidate.email=document.getElementById("e_email").value.trim();candidate.adresse=document.getElementById("e_adresse").value.trim();candidate.ville=document.getElementById("e_ville").value.trim();candidate.type=document.getElementById("e_type").value.trim();candidate.surface=document.getElementById("e_surface").value.trim()===""?"":safeNum(document.getElementById("e_surface").value,0);candidate.budget=document.getElementById("e_budget").value.trim()===""?0:safeNum(document.getElementById("e_budget").value,0);candidate.responsable=document.getElementById("e_responsable").value.trim();candidate.statut=document.getElementById("e_statut").value;candidate.dateDebut=document.getElementById("e_dateDebut").value;candidate.dateFinPrevue=document.getElementById("e_dateFinPrevue").value;candidate.notes=document.getElementById("e_notes").value.trim();
if(!candidate.ref||!candidate.nom||!candidate.client){flash("Référence, nom du projet et client sont obligatoires.","error");return;}
if(S.projects.some(x=>x.id!==projectId&&x.ref.toLowerCase()===candidate.ref.toLowerCase())){flash("La référence projet doit rester unique.","error");return;}
if(!validateEmail(candidate.email)){flash("L’adresse email n’est pas valide.","error");return;}
candidate.updatedAt=nowISO();const idx=S.projects.findIndex(x=>x.id===projectId);S.projects[idx]=candidate;S.documents=S.documents.map(d=>d.projectId===projectId?{...d,projectName:candidate.nom,updatedAt:nowISO()}:d);saveState();flash("Projet mis à jour.");}
function deleteProject(projectId){const p=projectById(projectId);if(!p) return;const ok=confirm(`Supprimer le projet "${p.ref} — ${p.nom}" et ses documents liés ?`);if(!ok) return;S.projects=S.projects.filter(x=>x.id!==projectId);S.documents=S.documents.filter(x=>x.projectId!==projectId);S.route={name:"dashboard",projectId:null};saveState();flash("Projet supprimé.");}
function exportSingleProject(projectId){const p=projectById(projectId);if(!p) return;const payload={project:p,documents:docsForProject(projectId),exportedAt:nowISO(),version:1};const blob=new Blob([JSON.stringify(payload,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`${p.ref||"projet"}-export.json`;a.click();URL.revokeObjectURL(a.href);flash("Projet exporté.");}
function createPlaceholderDoc(projectId,type){const p=projectById(projectId);if(!p) return;const prefixes={audit:"AUD",cr:"CR",devis:"DEV"};const names={audit:"Audit",cr:"Compte rendu",devis:"Devis"};const version=docsForProject(projectId).filter(d=>d.type===type).length+1;S.documents.unshift({id:uid("doc"),type,projectId,projectName:p.nom,documentRef:`${prefixes[type]}-${p.ref}-V${version}`,name:`${names[type]} — Sprint 2`,version,status:"brouillon",createdAt:nowISO(),updatedAt:nowISO()});p.updatedAt=nowISO();saveState();flash(`${names[type]} placeholder créé. Le vrai module vient au sprint 2.`);}
function docTypeLabel(type){if(type==="audit") return "Audit";if(type==="cr") return "CR";if(type==="devis") return "Devis";return type;}
function projectPageHTML(projectId){const p=projectById(projectId);if(!p){return `${flashHTML()}<div class="card"><div class="empty">Projet introuvable.</div><div class="toolbar" style="margin-top:14px"><button class="btn-secondary" onclick="setRoute('dashboard')">Retour dashboard</button></div></div>`;}const docs=docsForProject(projectId);return `${flashHTML()}<div class="topbar"><div><h2>${esc(p.ref)} — ${esc(p.nom)}</h2><p>${esc(p.client)} · ${esc(p.responsable||"Sans responsable")} · ${esc(p.ville||"—")}</p><div class="project-header-meta"><span class="badge ${statusClass(p.statut)}">${esc(p.statut)}</span><span class="badge neutral">Créé ${esc(relativeDateLabel(p.createdAt))}</span><span class="badge neutral">Mis à jour ${esc(relativeDateLabel(p.updatedAt))}</span></div></div><div class="top-actions"><button class="btn-secondary" onclick="setRoute('dashboard')">Retour dashboard</button><button class="btn-secondary" onclick="exportSingleProject('${p.id}')">Exporter projet</button><button class="btn-danger" onclick="deleteProject('${p.id}')">Supprimer projet</button></div></div><div class="grid grid-2"><div class="card"><div class="section-head"><h3>Infos projet</h3><span class="muted small">Objet central de l'app</span></div>${projectEditFormHTML(p)}</div><div class="grid"><div class="card" id="doc-actions-card"><div class="section-head"><h3>Créer un document</h3><span class="muted small">Sprint 2 = modules complets</span></div><div class="toolbar"><button class="btn-primary" onclick="createPlaceholderDoc('${p.id}','audit')">Créer Audit</button><button class="btn-secondary" onclick="createPlaceholderDoc('${p.id}','cr')">Créer CR</button><button class="btn-secondary" onclick="createPlaceholderDoc('${p.id}','devis')">Créer Devis</button></div></div><div class="card"><div class="section-head"><h3>Résumé</h3><span class="muted small">Vue rapide</span></div><div class="stat-grid"><div class="stat-box"><div class="t">Budget</div><div class="v">${formatMoney(p.budget||0)}</div></div><div class="stat-box"><div class="t">Surface</div><div class="v">${p.surface===""?"—":`${esc(String(p.surface))} m²`}</div></div><div class="stat-box"><div class="t">Date début</div><div class="v">${esc(p.dateDebut||"—")}</div></div><div class="stat-box"><div class="t">Date fin prévue</div><div class="v">${esc(p.dateFinPrevue||"—")}</div></div></div></div></div></div><div class="card" style="margin-top:18px"><div class="section-head"><h3>Documents du projet</h3><span class="muted small">${docs.length} document(s)</span></div>${docs.length?`<div class="table-wrap"><table><thead><tr><th>Type</th><th>Référence doc</th><th>Nom</th><th>Version</th><th>Statut</th><th>Date création</th><th>Mise à jour</th></tr></thead><tbody>${docs.map(d=>`<tr><td>${esc(docTypeLabel(d.type))}</td><td>${esc(d.documentRef||"—")}</td><td>${esc(d.name||"—")}</td><td>V${esc(String(d.version||1))}</td><td><span class="badge ${d.status==="final"?"ok":"warn"}">${esc(d.status||"brouillon")}</span></td><td>${esc(relativeDateLabel(d.createdAt))}</td><td>${esc(relativeDateLabel(d.updatedAt))}</td></tr>`).join("")}</tbody></table></div>`:`<div class="empty">Aucun document lié à ce projet pour l’instant.</div>`}</div>`;}
function scrollToDocActions(){document.getElementById("doc-actions-card")?.scrollIntoView({behavior:"smooth",block:"start"});}
function layout(inner){return `<div class="page"><aside class="sidebar"><div class="brand"><div class="brand-badge">ZB</div><div class="brand-copy"><h1>ZAF BAT</h1><p>Sprint 1 — JS statique</p></div></div><div class="nav"><button class="${S.route.name==="dashboard"?"active":""}" onclick="setRoute('dashboard')">Tableau de bord</button><button class="${S.route.name==="project"?"active":""}" onclick="${S.route.projectId?`setRoute('project','${S.route.projectId}')`:`setRoute('dashboard')`}">Fiche projet</button></div><div class="sidebar-note">GitHub Pages friendly. Stockage local avec export/import JSON. Sprint 1 couvre dashboard, fiche projet, schéma Project v2 et historique documentaire basique.</div></aside><main class="main">${inner}</main></div>`;}
let flashTimeout=null;
function render(){const app=document.getElementById("app");if(flashTimeout) clearTimeout(flashTimeout);let inner=S.route.name==="project"&&S.route.projectId?projectPageHTML(S.route.projectId):dashboardHTML();app.innerHTML=layout(inner);if(S.flash){flashTimeout=setTimeout(()=>{S.flash=null;render();},3200);}}
window.createProjectFromForm=createProjectFromForm;window.resetProjectForm=resetProjectForm;window.updateSearch=updateSearch;window.updateStatusFilter=updateStatusFilter;window.updateSort=updateSort;window.focusNewProject=focusNewProject;window.openProject=openProject;window.setRoute=setRoute;window.importBackup=importBackup;window.exportBackup=exportBackup;window.saveProjectEdits=saveProjectEdits;window.deleteProject=deleteProject;window.exportSingleProject=exportSingleProject;window.createPlaceholderDoc=createPlaceholderDoc;window.scrollToDocActions=scrollToDocActions;
loadState();render();
